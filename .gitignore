__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
*.egg-info/

# Raw data (data.json in docs/ is the built output and is NOT ignored)
*.csv

# Claude Code local settings
.claude/settings.local.json

# Agent skills: machine-local symlinks (absolute paths) into .agents/skills/,
# regenerated per-machine via `npx skills experimental_install`
.claude/skills/

# Credentials
*.json.key
service-account*.json
.env
